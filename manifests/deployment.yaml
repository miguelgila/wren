apiVersion: v1
kind: Namespace
metadata:
  name: bubo-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bubo-controller
  namespace: bubo-system
  labels:
    app.kubernetes.io/name: bubo-controller
    app.kubernetes.io/component: controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bubo-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bubo-controller
        app.kubernetes.io/component: controller
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: bubo-controller
      containers:
        - name: controller
          image: bubo-controller:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: metrics
              containerPort: 8080
              protocol: TCP
          env:
            - name: RUST_LOG
              value: "bubo_controller=info,kube=warn"
          livenessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: metrics
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
