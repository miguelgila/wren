apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: wrenqueues.hpc.cscs.ch
spec:
  group: hpc.cscs.ch
  names:
    categories: []
    kind: WrenQueue
    plural: wrenqueues
    shortNames: []
    singular: wrenqueue
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.maxNodes
      name: MaxNodes
      type: integer
    - jsonPath: .spec.maxWalltime
      name: MaxWalltime
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for WrenQueueSpec via `CustomResource`
        properties:
          spec:
            description: WrenQueue defines a scheduling queue with policies.
            properties:
              backfill:
                description: Backfill configuration
                nullable: true
                properties:
                  enabled:
                    type: boolean
                  lookAhead:
                    description: How far ahead to project resource availability
                    nullable: true
                    type: string
                required:
                - enabled
                type: object
              defaultPriority:
                default: 50
                description: Default priority for jobs without explicit priority
                format: int32
                type: integer
              fairShare:
                description: Fair-share configuration
                nullable: true
                properties:
                  decayHalfLife:
                    description: Usage history decay half-life (e.g., "7d")
                    nullable: true
                    type: string
                  enabled:
                    type: boolean
                required:
                - enabled
                type: object
              maxJobsPerUser:
                description: Maximum concurrent jobs per user
                format: uint32
                minimum: 0.0
                nullable: true
                type: integer
              maxNodes:
                description: Maximum total nodes this queue can consume
                format: uint32
                minimum: 0.0
                type: integer
              maxWalltime:
                description: Maximum walltime allowed for jobs in this queue
                nullable: true
                type: string
            required:
            - maxNodes
            type: object
        required:
        - spec
        title: WrenQueue
        type: object
    served: true
    storage: true
    subresources: {}
