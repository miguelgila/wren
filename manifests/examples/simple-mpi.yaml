apiVersion: wren.giar.dev/v1alpha1
kind: WrenJob
metadata:
  name: hello-mpi
spec:
  queue: default
  nodes: 2
  tasksPerNode: 1
  walltime: "30m"
  container:
    image: busybox:latest
    command: ["echo"]
    args: ["Hello from MPI rank"]
