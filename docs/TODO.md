 - [ ] wrenCLI: I want to be able to show the logs of a job by using the jobid, and also show extended job details (like sinfo) with it.
 - [x] Reaper ↔ Wren user identity integration (COMPLETE — Phase 3a):
   **Context:** Wren's container backend already sets `securityContext.runAsUser/runAsGroup` and injects `USER`/`HOME`/`LOGNAME` env vars from the WrenUser CRD. The Reaper backend receives a `UserIdentity` struct (username, uid, gid, supplemental_groups, home_dir) but currently ignores it — the `_user` parameter in `ReaperBackend::launch()` is unused. Reaper itself already supports running commands as other users.
   **What needs to change:**
   1. **`ReaperJobRequest` struct** (`crates/wren-controller/src/reaper.rs`): Add optional fields `uid: Option<u32>`, `gid: Option<u32>`, `username: Option<String>`, `home_dir: Option<String>`, `supplemental_groups: Option<Vec<u32>>`.
   2. **`ReaperBackend::launch()`** (`crates/wren-controller/src/reaper.rs`): Stop ignoring the `_user` parameter. When `Some(user)`, populate the new fields on `ReaperJobRequest` from `UserIdentity`. Also inject `USER`, `HOME`, `LOGNAME` into the environment map (same as the container backend does).
   3. **Reaper agent side** (separate repo: `github.com/miguelgila/reaper`): When the job request includes `uid`/`gid`, use `std::os::unix::process::CommandExt::uid()`/`.gid()` to drop privileges before exec. Set `supplemental_groups` if provided. Reaper already runs commands as other users, so this extends the existing mechanism to accept identity from Wren rather than relying on local configuration.
   4. **Tests**: Add unit tests for `build_job_request()` verifying that `UserIdentity` fields are correctly mapped to `ReaperJobRequest`. Add an integration test in `scripts/run-integration-tests.sh` (under user tests section) if a Reaper agent is available.
   **Why:** Without this, Reaper jobs run as whatever user the Reaper agent runs as (typically root). With this, Reaper jobs run as the correct Unix user matching the WrenUser CRD — same identity semantics as the container backend. Files written to shared filesystems (NFS, Lustre, GPFS) will have correct ownership.
   **Not needed:** No LDAP/NSS/PAM on compute nodes. Identity flows entirely from Wren controller → Reaper agent via the job request. Nodes stay stateless from an identity perspective.
 - [ ] Phase 3b — Multi-task support in Reaper backend: extend ReaperJobRequest with tasks_per_node/cpus_per_task/gpus_per_task, implement multi-process spawning with CPU affinity and GPU binding in Reaper agent. See wren/CLAUDE.md Phase 3b for full details.
 - [ ] Multi-tenancy namespacing: evaluate making WrenUser, WrenQueue, and other resources namespace-scoped (or adding namespace-aware access control) so that different teams/projects can have isolated user registries, queue configurations, and resource quotas within the same cluster. Currently WrenUser is cluster-scoped and WrenQueue/WrenJob are namespaced — consider whether WrenUser should support per-namespace overrides or stay cluster-wide with namespace-level RBAC restrictions.
 - [ ] Node partitioning (Slurm-style partitions): allow assigning nodes to specific queues or namespaces so that a team's jobs are scheduled only onto their designated nodes. Implementation approach: add an optional `nodeSelector` or `nodeAffinity` field to `WrenQueue` spec; the scheduler would then restrict placement to nodes matching those labels. This is the Slurm partition equivalent — e.g., "these 20 GPU nodes belong to the `ml-team` queue." Wren already has namespace-level multi-tenancy via Kubernetes RBAC; node partitioning completes the resource isolation picture. Could also support shared/preemptible overflow where idle partitioned nodes are available to other queues at lower priority.
