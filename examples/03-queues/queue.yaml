# Define a queue with resource limits and policies.
apiVersion: wren.scops-hpc.com/v1alpha1
kind: WrenQueue
metadata:
  name: batch
spec:
  maxNodes: 8
  maxWalltime: "1h"
  maxJobsPerUser: 3
  defaultPriority: 50
