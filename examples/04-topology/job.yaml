# Topology-aware placement: prefer nodes on the same network switch.
# On the kind test cluster, nodes are labeled with:
#   topology.wren.giar.dev/switch: switch-0 | switch-1
#   topology.wren.giar.dev/rack:   rack-0   | rack-1
apiVersion: wren.giar.dev/v1alpha1
kind: WrenJob
metadata:
  name: topology-aware-job
  annotations:
    wren.giar.dev/user: demo-user
spec:
  nodes: 2
  walltime: "10m"
  container:
    image: busybox:latest
    command: ["sh", "-c"]
    args: ["echo 'Placed on co-located nodes' && sleep 5"]
  topology:
    preferSameSwitch: true
    maxHops: 2
