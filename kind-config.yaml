# kind cluster configuration for Wren integration tests.
#
# Creates a 4-worker-node cluster with topology labels to test:
#   - Gang scheduling across multiple nodes
#   - Topology-aware placement (switch groups, racks)
#   - Resource availability and allocation
#
# Usage:
#   kind create cluster --name wren-test --config kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
    labels:
      topology.wren.giar.dev/switch: switch-0
      topology.wren.giar.dev/rack: rack-0
      topology.kubernetes.io/zone: zone-a
  - role: worker
    labels:
      topology.wren.giar.dev/switch: switch-0
      topology.wren.giar.dev/rack: rack-0
      topology.kubernetes.io/zone: zone-a
  - role: worker
    labels:
      topology.wren.giar.dev/switch: switch-1
      topology.wren.giar.dev/rack: rack-1
      topology.kubernetes.io/zone: zone-b
  - role: worker
    labels:
      topology.wren.giar.dev/switch: switch-1
      topology.wren.giar.dev/rack: rack-1
      topology.kubernetes.io/zone: zone-b
