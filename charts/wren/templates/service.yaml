{{- if .Values.metrics.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "wren.fullname" . }}-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "wren.labels" . | nindent 4 }}
    app.kubernetes.io/component: metrics
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "wren.selectorLabels" . | nindent 4 }}
{{- end }}
